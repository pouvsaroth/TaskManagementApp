<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TaskManagementApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/nav_background</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="colorPrimary">@color/nav_selected</item>
        <item name="colorAccent">@color/nav_selected</item>
        <item name="colorControlActivated">@color/nav_selected</item>
        <item name="android:colorBackground">@color/app_main_background</item>
        <item name="colorSurface">@color/card_background_color</item>
    </style>

    <style name="Theme.TaskManagementApp" parent="Base.Theme.TaskManagementApp">
        <!-- Add any theme-specific overrides here -->
        <item name="editTextColor">@color/text_primary</item>
    </style>
</resources>